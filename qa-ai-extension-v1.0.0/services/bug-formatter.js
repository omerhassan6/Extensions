class BugFormatter{static formatAsMarkdown(e){return`# Bug Report: ${e.title}\n\n## Description\n${e.description}\n\n## Expected Behavior\n${e.expectedBehavior}\n\n## Actual Behavior\n${e.actualBehavior}\n\n## Reproduction Steps\n${e.reproductionSteps.map((e,t)=>`${t+1}. ${e}`).join("\n")}\n\n## Environment\n- Browser: ${e.environment.browser}\n- OS: ${e.environment.os}\n- Viewport: ${e.environment.viewport}\n- Device Pixel Ratio: ${e.environment.devicePixelRatio}\n\n## Severity\n${e.severity}\n\n## Affected Area\n${e.affectedArea||"N/A"}\n\n---\nGenerated by QA Analyzer Extension on ${(new Date).toISOString()}`}static formatAsJSON(e){return JSON.stringify(e,null,2)}static formatAsJiraMarkdown(e){return`h1. ${e.title}\n\nh2. Description\n${e.description}\n\nh2. Expected Result\n${e.expectedBehavior}\n\nh2. Actual Result\n${e.actualBehavior}\n\nh2. Steps to Reproduce\n${e.reproductionSteps.map((e,t)=>`# Step ${t+1}: ${e}`).join("\n")}\n\nh2. Environment\n* Browser: ${e.environment.browser}\n* OS: ${e.environment.os}\n* Viewport: ${e.environment.viewport}\n\nh2. Priority\n${e.severity}`}static formatForClickUp(e){return{name:e.title,description:e.description,priority:this.mapSeverityToPriority(e.severity),custom_fields:{expected_result:e.expectedBehavior,actual_result:e.actualBehavior,steps_to_reproduce:e.reproductionSteps.join("\n"),browser:e.environment.browser,os:e.environment.os}}}static mapSeverityToPriority(e){return{Critical:1,High:2,Medium:3,Low:4}[e]||3}static createCompleteReport(e,t={}){return{id:this.generateUUID(),title:e.title||"UI Issue Found",description:e.description||"",expectedBehavior:e.expectedBehavior||"",actualBehavior:e.actualBehavior||"",reproductionSteps:e.reproductionSteps||[],severity:e.severity||"Medium",environment:{browser:t.userAgent||"Unknown",os:this.detectOS(t.userAgent),viewport:t.viewport||"Unknown",devicePixelRatio:t.devicePixelRatio||1,timestamp:(new Date).toISOString()},screenshot:{data:e.screenshot||null,annotated:e.annotatedScreenshot||null},issues:e.issues||[],metadata:t}}static detectOS(e){return e.includes("Windows")?"Windows":e.includes("Mac")?"macOS":e.includes("Linux")?"Linux":e.includes("Android")?"Android":e.includes("iPhone")?"iOS":"Unknown"}static generateUUID(){return"xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx".replace(/[xy]/g,function(e){const t=16*Math.random()|0;return("x"===e?t:3&t|8).toString(16)})}}"undefined"!=typeof module&&module.exports&&(module.exports=BugFormatter);